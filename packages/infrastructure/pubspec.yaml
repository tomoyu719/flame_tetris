name: tetris_infrastructure
description: Infrastructure layer for Flame Tetris - Repository implementations and external services
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.2

dependencies:
  flame_audio: ^2.1.0
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.0
  tetris_domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.0.0
