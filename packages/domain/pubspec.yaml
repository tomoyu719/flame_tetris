name: tetris_domain
description: Domain layer for Flame Tetris - Entities, Value Objects, and Service interfaces
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.2

dependencies:
  # Pure Dart - no Flutter dependencies
  meta: ^1.11.0

dev_dependencies:
  test: ^1.24.0
  very_good_analysis: ^10.0.0
